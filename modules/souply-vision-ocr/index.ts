// Native registration only (iOS Apple Vision OCR — VNRecognizeTextRequest).
// Autolinked into the iOS build via expo-module.config.json (platforms: apple).
// The JS that consumes the native module lives in `utils/visionOcr.ts` (so it can
// gracefully no-op on Android and on dev clients that predate the native build,
// exactly like components/LiquidGlass.tsx does for expo-glass-effect).
// Nothing to export here.
export {};
