// Native registration only (Android Material You CircularProgressIndicator).
// The React component that consumes it lives in app `components/MaterialProgress.tsx`
// (so it can fall back to ActivityIndicator on iOS). Nothing to export here.
export {};
