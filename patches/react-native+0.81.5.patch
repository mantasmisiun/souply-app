diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/drawable/BackgroundDrawable.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/drawable/BackgroundDrawable.kt
index 1ca242a..7cc2860 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/drawable/BackgroundDrawable.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/drawable/BackgroundDrawable.kt
@@ -118,7 +118,14 @@ internal class BackgroundDrawable(
       } else if (borderRadius?.hasRoundedBorders() != true) {
         canvas.drawRect(backgroundRect, backgroundPaint)
       } else {
-        canvas.drawPath(checkNotNull(backgroundRenderPath), backgroundPaint)
+        // PATCHED (souply): RN's allocation guard (updatePath) and this draw guard read
+        // different hasRoundedBorders sources (computed vs style), so the render path can be
+        // null here for non-uniform/elliptical radii (e.g. the bottomRight.vertical-only shape
+        // ComputedBorderRadius.hasRoundedBorders omits). checkNotNull then crashes the whole app
+        // ("Required value was null") mid-draw — seen on the store-resolution map zoom. Fall
+        // back to a plain rect fill instead of crashing.
+        backgroundRenderPath?.let { canvas.drawPath(it, backgroundPaint) }
+            ?: canvas.drawRect(backgroundRect, backgroundPaint)
       }
     }
 
@@ -134,7 +141,14 @@ internal class BackgroundDrawable(
       } else if (borderRadius?.hasRoundedBorders() != true) {
         canvas.drawRect(backgroundRect, backgroundPaint)
       } else {
-        canvas.drawPath(checkNotNull(backgroundRenderPath), backgroundPaint)
+        // PATCHED (souply): RN's allocation guard (updatePath) and this draw guard read
+        // different hasRoundedBorders sources (computed vs style), so the render path can be
+        // null here for non-uniform/elliptical radii (e.g. the bottomRight.vertical-only shape
+        // ComputedBorderRadius.hasRoundedBorders omits). checkNotNull then crashes the whole app
+        // ("Required value was null") mid-draw — seen on the store-resolution map zoom. Fall
+        // back to a plain rect fill instead of crashing.
+        backgroundRenderPath?.let { canvas.drawPath(it, backgroundPaint) }
+            ?: canvas.drawRect(backgroundRect, backgroundPaint)
       }
       backgroundPaint.setShader(null)
     }
diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/style/ComputedBorderRadius.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/style/ComputedBorderRadius.kt
index 234f04b..3b9c816 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/style/ComputedBorderRadius.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/uimanager/style/ComputedBorderRadius.kt
@@ -29,7 +29,12 @@ public data class ComputedBorderRadius(
         topRight.vertical > 0f ||
         bottomLeft.horizontal > 0f ||
         bottomLeft.vertical > 0f ||
-        bottomRight.horizontal > 0f
+        bottomRight.horizontal > 0f ||
+        // PATCHED (souply): upstream RN omits bottomRight.vertical here, so the
+        // BackgroundDrawable/BorderDrawable allocation guard (which keys off this) skips
+        // allocating the render path for a bottomRight.vertical-only (elliptical) radius while
+        // the draw guard still tries to draw it → checkNotNull crash. Include it for symmetry.
+        bottomRight.vertical > 0f
   }
 
   public fun isUniform(): Boolean {
